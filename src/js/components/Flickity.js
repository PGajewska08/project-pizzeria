class Flickity{
    constructor(elem, options){
            const thisFlickity = this;
            thisFlickity.elem = elem;
            thisFlickity.options = options;
        }
}

export default Flickity;